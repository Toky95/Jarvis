<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exemple.jarvisoffline">

    <!-- Micro pour l'écoute continue -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Appels directs -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <!-- Lecture des contacts pour "appelle Maman" -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <!-- Réveiller/allumer l'écran -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <!-- Service en premier plan (obligatoire pour tourner en continu) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- Se lancer automatiquement au démarrage du téléphone (optionnel) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Modifier le volume sonnerie -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Jarvis Offline"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".VoiceAssistantService"
            android:foregroundServiceType="microphone"
            android:exported="false"/>

    </application>
</manifest>
